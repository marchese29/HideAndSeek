# AUTO-GENERATED from FastAPI server — DO NOT EDIT
# Regenerate with: cd server && uv run python scripts/generate_openapi.py
openapi: 3.1.0
info:
  title: HideAndSeek
  description: Geographic Hide and Seek game server
  version: 0.1.0
paths:
  /maps:
    get:
      tags:
      - maps
      summary: List Maps
      description: List available maps with name, size, and region.
      operationId: list_maps_maps_get
      parameters:
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          description: Number of items to skip.
          default: 0
          title: Offset
        description: Number of items to skip.
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 500
          minimum: 1
          description: Maximum number of items to return.
          default: 100
          title: Limit
        description: Maximum number of items to return.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MapSummary'
                title: Response List Maps Maps Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /maps/{map_id}:
    get:
      tags:
      - maps
      summary: Get Map
      description: Full map detail including geometry. Omits stops/routes.
      operationId: get_map_maps__map_id__get
      parameters:
      - name: map_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Map Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MapDetail'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games:
    post:
      tags:
      - games
      summary: Create Game
      description: Create a new game on a map.
      operationId: create_game_games_post
      parameters:
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateGameRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GameResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/join:
    post:
      tags:
      - games
      summary: Join Game
      description: Join a game by its join code.
      operationId: join_game_games_join_post
      parameters:
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/JoinGameRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JoinGameResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}:
    get:
      tags:
      - games
      summary: Get Game State
      description: Fetch current game state.
      operationId: get_game_state_games__game_id__get
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GameResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/players/{player_id}:
    patch:
      tags:
      - games
      summary: Patch Player
      description: Update a player's role, name, or color.
      operationId: patch_player_games__game_id__players__player_id__patch
      parameters:
      - name: player_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Player Id
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PlayerUpdate'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlayerResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/start:
    post:
      tags:
      - games
      summary: Start Game
      description: Transition the game from lobby to hiding.
      operationId: start_game_games__game_id__start_post
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GameResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/end:
    post:
      tags:
      - games
      summary: End Game
      description: Transition the game to finished.
      operationId: end_game_games__game_id__end_post
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GameResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/map:
    get:
      tags:
      - games
      summary: Get Effective Map
      description: Effective map with transit data and exclusions applied.
      operationId: get_effective_map_games__game_id__map_get
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EffectiveMapResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/location:
    post:
      tags:
      - location
      summary: Report Location
      description: Report the caller's location and receive visible player positions.
      operationId: report_location_games__game_id__location_post
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationReportRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationReportResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/location-history:
    get:
      tags:
      - location
      summary: Location History
      description: Full location log for post-game replay. Only available when finished.
      operationId: location_history_games__game_id__location_history_get
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/LocationHistoryEntry'
                title: Response Location History Games  Game Id  Location History
                  Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/questions/radar:
    post:
      tags:
      - questions
      summary: Ask Radar Question
      description: Ask a radar question, spending a radar inventory slot.
      operationId: ask_radar_question_games__game_id__questions_radar_post
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AskRadarRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/questions/thermometer:
    post:
      tags:
      - questions
      summary: Ask Thermometer Question
      description: Ask a thermometer question, spending a thermometer inventory slot.
      operationId: ask_thermometer_question_games__game_id__questions_thermometer_post
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AskThermometerRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/questions/matching:
    post:
      tags:
      - questions
      summary: Ask Matching Question
      description: Ask a matching question about a feature category.
      operationId: ask_matching_question_games__game_id__questions_matching_post
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AskMatchingRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/questions/measuring:
    post:
      tags:
      - questions
      summary: Ask Measuring Question
      description: Ask a measuring question about a feature category.
      operationId: ask_measuring_question_games__game_id__questions_measuring_post
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AskMeasuringRequest'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/questions/thermometer/{question_id}/lock-in:
    post:
      tags:
      - questions
      summary: Lock In Question
      description: Lock in the seeker's end position for a thermometer question.
      operationId: lock_in_question_games__game_id__questions_thermometer__question_id__lock_in_post
      parameters:
      - name: question_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Question Id
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/questions/{question_id}/answer:
    post:
      tags:
      - questions
      summary: Answer Question
      description: "Hider answers a question \u2014 snapshot location, compute answer\
        \ and exclusion."
      operationId: answer_question_games__game_id__questions__question_id__answer_post
      parameters:
      - name: question_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Question Id
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuestionResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /games/{game_id}/questions:
    get:
      tags:
      - questions
      summary: List Game Questions
      description: Chronological list of all questions. Hider location hidden from
        seekers.
      operationId: list_game_questions_games__game_id__questions_get
      parameters:
      - name: game_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
          title: Game Id
      - name: x-client-id
        in: header
        required: true
        schema:
          type: string
          format: uuid
          title: X-Client-Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/QuestionResponse'
                title: Response List Game Questions Games  Game Id  Questions Get
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /:
    get:
      summary: Root
      operationId: root__get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
                title: Response Root  Get
  /health:
    get:
      summary: Health
      operationId: health_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties:
                  type: string
                type: object
                title: Response Health Health Get
components:
  schemas:
    AskMatchingRequest:
      properties:
        location:
          $ref: '#/components/schemas/Point'
          description: Current seeker position as a GeoJSON Point.
        category:
          $ref: '#/components/schemas/FeatureCategory'
          description: Feature category to match on.
        feature_class:
          anyOf:
          - type: integer
          - type: 'null'
          title: Feature Class
          description: Feature class tier. Required for classed categories.
      type: object
      required:
      - location
      - category
      title: AskMatchingRequest
      description: Ask a matching question about a feature category.
    AskMeasuringRequest:
      properties:
        location:
          $ref: '#/components/schemas/Point'
          description: Current seeker position as a GeoJSON Point.
        category:
          $ref: '#/components/schemas/FeatureCategory'
          description: Feature category to measure distance to.
        feature_class:
          anyOf:
          - type: integer
          - type: 'null'
          title: Feature Class
          description: Feature class tier. Required for classed categories.
      type: object
      required:
      - location
      - category
      title: AskMeasuringRequest
      description: Ask a measuring question about a feature category.
    AskRadarRequest:
      properties:
        location:
          $ref: '#/components/schemas/Point'
          description: Current seeker position as a GeoJSON Point.
        slot_index:
          type: integer
          title: Slot Index
          description: 0-based index into the available radar slot list.
        custom_distance:
          anyOf:
          - type: number
          - type: 'null'
          title: Custom Distance
          description: Required for custom slots (distance=null). In convention units.
      type: object
      required:
      - location
      - slot_index
      title: AskRadarRequest
      description: Ask a radar question, spending a radar inventory slot.
    AskThermometerRequest:
      properties:
        location:
          $ref: '#/components/schemas/Point'
          description: Current seeker position as a GeoJSON Point.
        slot_index:
          type: integer
          title: Slot Index
          description: 0-based index into the available thermometer slot list.
        custom_distance:
          anyOf:
          - type: number
          - type: 'null'
          title: Custom Distance
          description: Required for custom slots (distance=null). In convention units.
      type: object
      required:
      - location
      - slot_index
      title: AskThermometerRequest
      description: Ask a thermometer question, spending a thermometer inventory slot.
    CategoryUsageResponse:
      properties:
        category:
          type: string
          title: Category
        feature_class:
          anyOf:
          - type: integer
          - type: 'null'
          title: Feature Class
        consumed_at:
          type: string
          format: date-time
          title: Consumed At
      type: object
      required:
      - category
      - consumed_at
      title: CategoryUsageResponse
      description: A used matching or measuring category.
    CreateGameRequest:
      properties:
        map_id:
          type: string
          format: uuid
          title: Map Id
          description: ID of the map to play on.
        device_token:
          anyOf:
          - type: string
          - type: 'null'
          title: Device Token
          description: Hex-encoded APNS device token. Optional for game creation.
        device_token_environment:
          type: string
          title: Device Token Environment
          description: 'APNS environment: "production" or "sandbox".'
          default: production
      type: object
      required:
      - map_id
      title: CreateGameRequest
      description: Create a new game on a map.
    EffectiveMapResponse:
      properties:
        name:
          type: string
          title: Name
        size:
          $ref: '#/components/schemas/MapSize'
        boundary:
          $ref: '#/components/schemas/Polygon'
          description: GeoJSON Polygon.
        districts:
          items: {}
          type: array
          title: Districts
        district_classes:
          items: {}
          type: array
          title: District Classes
        stops:
          items:
            $ref: '#/components/schemas/StopResponse'
          type: array
          title: Stops
        routes:
          items:
            $ref: '#/components/schemas/RouteResponse'
          type: array
          title: Routes
      type: object
      required:
      - name
      - size
      - boundary
      - districts
      - district_classes
      - stops
      - routes
      title: EffectiveMapResponse
      description: 'The game map with transit data and exclusions applied.


        Single source of truth for what the client should render.'
    FeatureCategory:
      type: string
      enum:
      - transit_line
      - administrative_area
      - landmass
      - high_speed_train_line
      - rail_station
      - international_border
      - admin_division_border
      - coastline
      - commercial_airport
      - mountain
      - park
      - amusement_park
      - zoo
      - aquarium
      - golf_course
      - museum
      - movie_theater
      - hospital
      - library
      - foreign_consulate
      title: FeatureCategory
    FeatureParamsResponse:
      properties:
        type:
          type: string
          enum:
          - matching
          - measuring
          title: Type
        category:
          type: string
          title: Category
          description: Feature category.
        feature_class:
          anyOf:
          - type: integer
          - type: 'null'
          title: Feature Class
          description: Feature class tier, if applicable.
        source:
          type: string
          title: Source
          description: Data source (e.g. map_data).
        seeker_resolution:
          $ref: '#/components/schemas/FeatureResolution'
          description: Seeker feature resolution.
        hider_resolution:
          anyOf:
          - $ref: '#/components/schemas/FeatureResolution'
          - type: 'null'
          description: Hider feature resolution (populated at answer time).
      type: object
      required:
      - type
      - category
      - source
      - seeker_resolution
      title: FeatureParamsResponse
      description: Parameters for a matching or measuring question.
    FeatureResolution:
      properties:
        feature_id:
          type: string
          title: Feature Id
          description: Stable identifier of the resolved feature.
        name:
          type: string
          title: Name
          description: Human-readable name.
        distance:
          type: number
          title: Distance
          description: Distance in convention units.
      type: object
      required:
      - feature_id
      - name
      - distance
      title: FeatureResolution
      description: Resolution result for one player's feature lookup.
    GameResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        map_id:
          type: string
          format: uuid
          title: Map Id
        status:
          $ref: '#/components/schemas/GameStatus'
        convention:
          type: string
          title: Convention
          description: 'Distance convention: "metric" or "imperial".'
        join_code:
          anyOf:
          - type: string
          - type: 'null'
          title: Join Code
          description: 4-character code for joining. Null after game ends.
        timing:
          additionalProperties: true
          type: object
          title: Timing
          description: 'TimingRules: hiding_time_min, rest_periods, etc.'
        inventory:
          $ref: '#/components/schemas/InventoryResponse'
          description: Question inventory (slots + category usage).
        players:
          items:
            $ref: '#/components/schemas/PlayerResponse'
          type: array
          title: Players
        created_at:
          type: string
          format: date-time
          title: Created At
        hiding_started_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Hiding Started At
          description: When the hiding phase began. Clients compute countdown.
        seeking_started_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Seeking Started At
          description: When the seeking phase began.
      type: object
      required:
      - id
      - map_id
      - status
      - convention
      - join_code
      - timing
      - inventory
      - players
      - created_at
      title: GameResponse
      description: Full game state, including players and inventory.
    GameStatus:
      type: string
      enum:
      - lobby
      - hiding
      - seeking
      - endgame
      - finished
      title: GameStatus
    GeometryCollection:
      properties:
        bbox:
          anyOf:
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 4
            minItems: 4
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 6
            minItems: 6
          - type: 'null'
          title: Bbox
        type:
          type: string
          const: GeometryCollection
          title: Type
        geometries:
          items:
            oneOf:
            - $ref: '#/components/schemas/Point'
            - $ref: '#/components/schemas/MultiPoint'
            - $ref: '#/components/schemas/LineString'
            - $ref: '#/components/schemas/MultiLineString'
            - $ref: '#/components/schemas/Polygon'
            - $ref: '#/components/schemas/MultiPolygon'
            - $ref: '#/components/schemas/GeometryCollection'
            discriminator:
              propertyName: type
              mapping:
                GeometryCollection: '#/components/schemas/GeometryCollection'
                LineString: '#/components/schemas/LineString'
                MultiLineString: '#/components/schemas/MultiLineString'
                MultiPoint: '#/components/schemas/MultiPoint'
                MultiPolygon: '#/components/schemas/MultiPolygon'
                Point: '#/components/schemas/Point'
                Polygon: '#/components/schemas/Polygon'
          type: array
          title: Geometries
      type: object
      required:
      - type
      - geometries
      title: GeometryCollection
      description: GeometryCollection Model
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    InventoryResponse:
      properties:
        radar_slots:
          items:
            $ref: '#/components/schemas/SlotResponse'
          type: array
          title: Radar Slots
          description: Radar question slots.
        thermometer_slots:
          items:
            $ref: '#/components/schemas/SlotResponse'
          type: array
          title: Thermometer Slots
          description: Thermometer question slots.
        matching_used:
          items:
            $ref: '#/components/schemas/CategoryUsageResponse'
          type: array
          title: Matching Used
          description: Matching categories already used.
        measuring_used:
          items:
            $ref: '#/components/schemas/CategoryUsageResponse'
          type: array
          title: Measuring Used
          description: Measuring categories already used.
      type: object
      required:
      - radar_slots
      - thermometer_slots
      - matching_used
      - measuring_used
      title: InventoryResponse
      description: Structured question inventory for a game.
    JoinGameRequest:
      properties:
        join_code:
          type: string
          title: Join Code
          description: 4-character code shared by the host.
        name:
          type: string
          title: Name
          description: Display name for this player.
        color:
          type: string
          title: Color
          description: Hex color for this player, e.g. "#FF5733".
        device_token:
          type: string
          title: Device Token
          description: "Hex-encoded APNS device token. Required \u2014 push is central\
            \ to gameplay."
        device_token_environment:
          type: string
          title: Device Token Environment
          description: 'APNS environment: "production" or "sandbox".'
          default: production
      type: object
      required:
      - join_code
      - name
      - color
      - device_token
      title: JoinGameRequest
      description: Join an existing game by its join code.
    JoinGameResponse:
      properties:
        game:
          $ref: '#/components/schemas/GameResponse'
        player_id:
          type: string
          format: uuid
          title: Player Id
          description: The joining player's ID for subsequent requests.
      type: object
      required:
      - game
      - player_id
      title: JoinGameResponse
      description: "Returned when a player joins a game \u2014 includes the game state\
        \ and the caller's player ID."
    LineString:
      properties:
        bbox:
          anyOf:
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 4
            minItems: 4
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 6
            minItems: 6
          - type: 'null'
          title: Bbox
        type:
          type: string
          const: LineString
          title: Type
        coordinates:
          items:
            anyOf:
            - $ref: '#/components/schemas/Position2D'
            - $ref: '#/components/schemas/Position3D'
          type: array
          minItems: 2
          title: Coordinates
      type: object
      required:
      - type
      - coordinates
      title: LineString
      description: LineString Model
    LocationHistoryEntry:
      properties:
        player_id:
          type: string
          format: uuid
          title: Player Id
        coordinates:
          $ref: '#/components/schemas/Point'
          description: GeoJSON Point.
        timestamp:
          type: string
          format: date-time
          title: Timestamp
      type: object
      required:
      - player_id
      - coordinates
      - timestamp
      title: LocationHistoryEntry
      description: A single location update in the post-game replay log.
    LocationReportRequest:
      properties:
        coordinates:
          $ref: '#/components/schemas/Point'
          description: Current position as a GeoJSON Point.
        timestamp:
          type: string
          format: date-time
          title: Timestamp
          description: Client-side timestamp of the reading.
      type: object
      required:
      - coordinates
      - timestamp
      title: LocationReportRequest
      description: Report the caller's current position.
    LocationReportResponse:
      properties:
        players:
          items:
            $ref: '#/components/schemas/VisiblePlayer'
          type: array
          title: Players
      type: object
      required:
      - players
      title: LocationReportResponse
      description: "Returned after reporting location \u2014 includes positions of\
        \ all visible players."
    MapDetail:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        size:
          $ref: '#/components/schemas/MapSize'
        transit_dataset_id:
          type: string
          format: uuid
          title: Transit Dataset Id
        boundary:
          $ref: '#/components/schemas/Polygon'
          description: GeoJSON Polygon defining the playable area.
        districts:
          items: {}
          type: array
          title: Districts
          description: District boundaries with id, name, class, and geometry.
        district_classes:
          items: {}
          type: array
          title: District Classes
          description: District class definitions (tier + label).
        default_inventory:
          additionalProperties: true
          type: object
          title: Default Inventory
          description: Default question inventory for games on this map.
        notes:
          anyOf:
          - type: string
          - type: 'null'
          title: Notes
      type: object
      required:
      - id
      - name
      - size
      - transit_dataset_id
      - boundary
      - districts
      - district_classes
      - default_inventory
      - notes
      title: MapDetail
      description: Full map detail including geometry for rendering a preview. Omits
        stops/routes.
    MapSize:
      type: string
      enum:
      - small
      - medium
      - large
      - special
      title: MapSize
    MapSummary:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        size:
          $ref: '#/components/schemas/MapSize'
        region:
          type: string
          title: Region
          description: Geographic region from the transit dataset.
      type: object
      required:
      - id
      - name
      - size
      - region
      title: MapSummary
      description: "A map in the browse list \u2014 name, size, and region."
    MultiLineString:
      properties:
        bbox:
          anyOf:
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 4
            minItems: 4
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 6
            minItems: 6
          - type: 'null'
          title: Bbox
        type:
          type: string
          const: MultiLineString
          title: Type
        coordinates:
          items:
            items:
              anyOf:
              - $ref: '#/components/schemas/Position2D'
              - $ref: '#/components/schemas/Position3D'
            type: array
            minItems: 2
          type: array
          title: Coordinates
      type: object
      required:
      - type
      - coordinates
      title: MultiLineString
      description: MultiLineString Model
    MultiPoint:
      properties:
        bbox:
          anyOf:
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 4
            minItems: 4
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 6
            minItems: 6
          - type: 'null'
          title: Bbox
        type:
          type: string
          const: MultiPoint
          title: Type
        coordinates:
          items:
            anyOf:
            - $ref: '#/components/schemas/Position2D'
            - $ref: '#/components/schemas/Position3D'
          type: array
          title: Coordinates
      type: object
      required:
      - type
      - coordinates
      title: MultiPoint
      description: MultiPoint Model
    MultiPolygon:
      properties:
        bbox:
          anyOf:
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 4
            minItems: 4
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 6
            minItems: 6
          - type: 'null'
          title: Bbox
        type:
          type: string
          const: MultiPolygon
          title: Type
        coordinates:
          items:
            items:
              items:
                anyOf:
                - $ref: '#/components/schemas/Position2D'
                - $ref: '#/components/schemas/Position3D'
              type: array
              minItems: 4
            type: array
          type: array
          title: Coordinates
      type: object
      required:
      - type
      - coordinates
      title: MultiPolygon
      description: MultiPolygon Model
    PlayerResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        name:
          type: string
          title: Name
        color:
          type: string
          title: Color
          description: Hex color, e.g. "#FF5733".
        role:
          anyOf:
          - $ref: '#/components/schemas/PlayerRole'
          - type: 'null'
          description: Null until the host assigns a role.
      type: object
      required:
      - id
      - name
      - color
      - role
      title: PlayerResponse
      description: A player in a game.
    PlayerRole:
      type: string
      enum:
      - hider
      - seeker
      title: PlayerRole
    PlayerUpdate:
      properties:
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
          description: New display name.
        color:
          anyOf:
          - type: string
          - type: 'null'
          title: Color
          description: New hex color.
        role:
          anyOf:
          - $ref: '#/components/schemas/PlayerRole'
          - type: 'null'
          description: Assign hider or seeker role.
      type: object
      title: PlayerUpdate
      description: Partial update to a player. All fields are optional; only provided
        fields are applied.
    Point:
      properties:
        bbox:
          anyOf:
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 4
            minItems: 4
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 6
            minItems: 6
          - type: 'null'
          title: Bbox
        type:
          type: string
          const: Point
          title: Type
        coordinates:
          anyOf:
          - $ref: '#/components/schemas/Position2D'
          - $ref: '#/components/schemas/Position3D'
          title: Coordinates
      type: object
      required:
      - type
      - coordinates
      title: Point
      description: Point Model
    Polygon:
      properties:
        bbox:
          anyOf:
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 4
            minItems: 4
          - prefixItems:
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            - type: number
            type: array
            maxItems: 6
            minItems: 6
          - type: 'null'
          title: Bbox
        type:
          type: string
          const: Polygon
          title: Type
        coordinates:
          items:
            items:
              anyOf:
              - $ref: '#/components/schemas/Position2D'
              - $ref: '#/components/schemas/Position3D'
            type: array
            minItems: 4
          type: array
          title: Coordinates
      type: object
      required:
      - type
      - coordinates
      title: Polygon
      description: Polygon Model
    Position2D:
      prefixItems:
      - type: number
        title: Longitude
      - type: number
        title: Latitude
      type: array
      maxItems: 2
      minItems: 2
    Position3D:
      prefixItems:
      - type: number
        title: Longitude
      - type: number
        title: Latitude
      - type: number
        title: Altitude
      type: array
      maxItems: 3
      minItems: 3
    QuestionResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        game_id:
          type: string
          format: uuid
          title: Game Id
        sequence:
          type: integer
          title: Sequence
          description: 1-based chronological order within the game.
        question_type:
          $ref: '#/components/schemas/QuestionType'
        status:
          $ref: '#/components/schemas/QuestionStatus'
        parameters:
          anyOf:
          - $ref: '#/components/schemas/RadarParamsResponse'
          - $ref: '#/components/schemas/ThermometerParamsResponse'
          - $ref: '#/components/schemas/FeatureParamsResponse'
          title: Parameters
          description: Type-specific question parameters.
        asked_by:
          type: string
          format: uuid
          title: Asked By
          description: Player ID of the seeker who asked.
        asked_at:
          type: string
          format: date-time
          title: Asked At
        seeker_location_start:
          $ref: '#/components/schemas/Point'
          description: "GeoJSON Point \u2014 seeker position when asked."
        seeker_location_end:
          anyOf:
          - $ref: '#/components/schemas/Point'
          - type: 'null'
          description: "GeoJSON Point \u2014 seeker position at lock-in (thermometer\
            \ only)."
        answerable_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Answerable At
          description: When the question became answerable. Clients compute deadline.
        answered_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Answered At
        hider_location:
          anyOf:
          - $ref: '#/components/schemas/Point'
          - type: 'null'
          description: "GeoJSON Point \u2014 hider position at answer time. Hidden\
            \ from seekers."
        answer:
          anyOf:
          - type: string
          - type: 'null'
          title: Answer
          description: yes/no for radar, closer/farther for thermometer.
        exclusion:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/Point'
            - $ref: '#/components/schemas/MultiPoint'
            - $ref: '#/components/schemas/LineString'
            - $ref: '#/components/schemas/MultiLineString'
            - $ref: '#/components/schemas/Polygon'
            - $ref: '#/components/schemas/MultiPolygon'
            - $ref: '#/components/schemas/GeometryCollection'
            discriminator:
              propertyName: type
              mapping:
                GeometryCollection: '#/components/schemas/GeometryCollection'
                LineString: '#/components/schemas/LineString'
                MultiLineString: '#/components/schemas/MultiLineString'
                MultiPoint: '#/components/schemas/MultiPoint'
                MultiPolygon: '#/components/schemas/MultiPolygon'
                Point: '#/components/schemas/Point'
                Polygon: '#/components/schemas/Polygon'
          - type: 'null'
          title: Exclusion
          description: "GeoJSON geometry \u2014 the exclusion zone."
        total_exclusion:
          anyOf:
          - oneOf:
            - $ref: '#/components/schemas/Point'
            - $ref: '#/components/schemas/MultiPoint'
            - $ref: '#/components/schemas/LineString'
            - $ref: '#/components/schemas/MultiLineString'
            - $ref: '#/components/schemas/Polygon'
            - $ref: '#/components/schemas/MultiPolygon'
            - $ref: '#/components/schemas/GeometryCollection'
            discriminator:
              propertyName: type
              mapping:
                GeometryCollection: '#/components/schemas/GeometryCollection'
                LineString: '#/components/schemas/LineString'
                MultiLineString: '#/components/schemas/MultiLineString'
                MultiPoint: '#/components/schemas/MultiPoint'
                MultiPolygon: '#/components/schemas/MultiPolygon'
                Point: '#/components/schemas/Point'
                Polygon: '#/components/schemas/Polygon'
          - type: 'null'
          title: Total Exclusion
          description: "GeoJSON geometry \u2014 cumulative exclusion across all answered\
            \ questions."
      type: object
      required:
      - id
      - game_id
      - sequence
      - question_type
      - status
      - parameters
      - asked_by
      - asked_at
      - seeker_location_start
      - seeker_location_end
      - answered_at
      - hider_location
      - answer
      - exclusion
      - total_exclusion
      title: QuestionResponse
      description: "A question in the game \u2014 state machine: asked -> in_progress\
        \ -> answerable -> answered."
    QuestionStatus:
      type: string
      enum:
      - asked
      - in_progress
      - answerable
      - answered
      title: QuestionStatus
    QuestionType:
      type: string
      enum:
      - radar
      - thermometer
      - matching
      - measuring
      title: QuestionType
    RadarParamsResponse:
      properties:
        type:
          type: string
          const: radar
          title: Type
          default: radar
        radius:
          type: number
          title: Radius
          description: Radar radius in convention units.
      type: object
      required:
      - radius
      title: RadarParamsResponse
      description: Parameters for a radar question.
    RouteResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        stable_id:
          type: string
          title: Stable Id
          description: Stable identifier from the transit dataset.
        name:
          type: string
          title: Name
        color:
          type: string
          title: Color
          description: Hex color for rendering.
        route_type:
          type: string
          title: Route Type
          description: metro, bus, tram, rail, or ferry.
        shape:
          $ref: '#/components/schemas/LineString'
          description: GeoJSON LineString.
        stop_ids:
          items:
            type: string
            format: uuid
          type: array
          title: Stop Ids
          description: Ordered stop IDs along this route.
      type: object
      required:
      - id
      - stable_id
      - name
      - color
      - route_type
      - shape
      - stop_ids
      title: RouteResponse
      description: A transit route on the effective game map, with ordered stop IDs.
    SlotResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        slot_index:
          type: integer
          title: Slot Index
          description: Original ordering index.
        distance:
          anyOf:
          - type: number
          - type: 'null'
          title: Distance
          description: Preset distance in convention units, or null for custom.
        consumed:
          type: boolean
          title: Consumed
          description: Whether this slot has been used.
      type: object
      required:
      - id
      - slot_index
      - distance
      - consumed
      title: SlotResponse
      description: A single inventory slot (radar or thermometer).
    StopResponse:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        stable_id:
          type: string
          title: Stable Id
          description: Stable identifier from the transit dataset.
        name:
          type: string
          title: Name
        coordinates:
          $ref: '#/components/schemas/Point'
          description: GeoJSON Point.
      type: object
      required:
      - id
      - stable_id
      - name
      - coordinates
      title: StopResponse
      description: A transit stop on the effective game map.
    ThermometerParamsResponse:
      properties:
        type:
          type: string
          const: thermometer
          title: Type
          default: thermometer
        min_travel:
          type: number
          title: Min Travel
          description: Minimum travel distance in convention units.
      type: object
      required:
      - min_travel
      title: ThermometerParamsResponse
      description: Parameters for a thermometer question.
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    VisiblePlayer:
      properties:
        player_id:
          type: string
          format: uuid
          title: Player Id
        name:
          type: string
          title: Name
        color:
          type: string
          title: Color
        role:
          anyOf:
          - $ref: '#/components/schemas/PlayerRole'
          - type: 'null'
        coordinates:
          $ref: '#/components/schemas/Point'
          description: "GeoJSON Point \u2014 latest reported position."
        timestamp:
          type: string
          format: date-time
          title: Timestamp
      type: object
      required:
      - player_id
      - name
      - color
      - role
      - coordinates
      - timestamp
      title: VisiblePlayer
      description: A player visible to the caller, with their latest position.
