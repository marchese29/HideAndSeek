[project]
name = "hideandseek-reconciler"
version = "0.1.0"
description = "Timer reconciler for HideAndSeek (polls Postgres, enqueues Celery tasks)"
authors = [
    { name = "Dan Marchese", email = "dan@marchese.me" }
]
requires-python = ">=3.12"
dependencies = [
    "hideandseek-core",
    "hideandseek-worker",
    "structlog>=25.5.0",
]

[tool.uv.sources]
hideandseek-core = { workspace = true }
hideandseek-worker = { workspace = true }

[dependency-groups]
dev = [
    "ruff>=0.11.0",
    "pyright>=1.1.0",
]

[tool.ruff]
extend = "../ruff-shared.toml"

[tool.ruff.lint.isort]
known-first-party = ["hideandseek_reconciler", "hideandseek_worker", "hideandseek_core", "hideandseek_models"]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "standard"

[build-system]
requires = ["uv_build>=0.10.0,<0.11.0"]
build-backend = "uv_build"
