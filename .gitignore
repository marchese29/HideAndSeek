# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# Cloud service credentials (downloaded JSON — live in Secrets Manager instead)
*firebase-adminsdk*.json
*-service-account*.json

# Claude Things
.claude/

# Beads
.beads/*.log*
# Lift bd.sock (unix socket) up to the root — EAS Build doesn't honor the
# nested .beads/.gitignore, and the socket crashes its tarball-copy step.
.beads/bd.sock
